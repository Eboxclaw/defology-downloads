# Defology developer previews

Public binary previews for the private Defology source repository.

These builds are for Hyperliquid testnet and paper practice only. The Android APK uses a debug signature. The macOS bundle is unsigned; use right-click, then Open. They are not production releases and do not enable mainnet or live order submission.

Verify SHA-256 values against the release notes before installing.
